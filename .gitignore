/target

# Staged Release Artifacts — where the Inno Setup script and make-portable.ps1
# write the Windows setup .exe and portable .zip
/dist

# Working notes and generated tickets — local scratch, not shared
/.scratch

# VS Code — keep only the shareable project config, ignore personal/local files
.vscode/*
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

/target

# Working notes and generated tickets — local scratch, not shared
/.scratch

# VS Code — keep only the shareable project config, ignore personal/local files
.vscode/*
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
